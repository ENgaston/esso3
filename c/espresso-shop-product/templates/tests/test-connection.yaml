apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "espresso.fullname" . }}-test-connection"
  labels:
    {{- include "espresso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "espresso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
